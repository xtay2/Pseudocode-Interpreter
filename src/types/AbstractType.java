package types;

import expressions.abstractions.Expression;

/**
 * This is a marker Interface for all Types. It gets used in every
 * {@link Expression}.
 * 
 * @see ExpressionType
 * @see SpecificType
 */
public interface AbstractType {
	// Marker Interface for all Types
}
